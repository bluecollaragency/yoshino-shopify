{% comment %} 
  To be used with the various feature tout sections.
  Make sure the data naming is the same across all these or this snippet wont work.
  Add any classes that include liquid to the safelist [in tailwind.config] or they wont get included.
{% endcomment %}
<div class="tw-w-full lg:tw-w-{{ block.settings.tout_width }} tw-px-4 slider__slide" id="Slide-template--featured-touter-{{ forloop.index }}">
  <div class="featured-tout tw-relative tw-flex tw-min-h-[540px] lg:tw-min-h-[{{ block.settings.tout_height }}] tw-bg-gray-800 tw-items-end">
    {% comment %} <img src="https://via.placeholder.com/500" alt="" class="tw-absolute tw-inset-0 tw-w-full tw-h-full tw-object-cover tw-z-0"> {% endcomment %}
    {%- if block.settings.tout_image != blank -%}
      <img
        srcset="{%- if block.settings.tout_image.width >= 165 -%}{{ block.settings.tout_image | image_url: width: 165 }} 165w,{%- endif -%}
        {%- if block.settings.tout_image.width >= 360 -%}{{ block.settings.tout_image | image_url: width: 360 }} 360w,{%- endif -%}
        {%- if block.settings.tout_image.width >= 535 -%}{{ block.settings.tout_image | image_url: width: 535 }} 535w,{%- endif -%}
        {%- if block.settings.tout_image.width >= 750 -%}{{ block.settings.tout_image | image_url: width: 750 }} 750w,{%- endif -%}
        {%- if block.settings.tout_image.width >= 1070 -%}{{ block.settings.tout_image | image_url: width: 1070 }} 1070w,{%- endif -%}
        {%- if block.settings.tout_image.width >= 1500 -%}{{ block.settings.tout_image | image_url: width: 1500 }} 1500w,{%- endif -%}
        {%- if block.settings.tout_image.width >= 2400 -%}{{ block.settings.tout_image | image_url: width: 2400 }} 2400w,{%- endif -%}
        {{ block.settings.tout_image | image_url }} {{ block.settings.tout_image.width }}w"
        src="{{ block.settings.tout_image | image_url: width: 1500 }}"
        sizes="100vw"
        alt="{{ block.settings.tout_image.alt | escape }}"
        loading="lazy"
        width="{{ block.settings.tout_image.width }}"
        height="{{ block.settings.tout_image.height }}"
        class="tw-absolute tw-inset-0 tw-w-full tw-h-full tw-object-cover tw-z-0"
      >
    {%- else -%}
      {{ 'image' | placeholder_svg_tag: 'tw-absolute tw-inset-0 tw-w-full tw-h-full tw-object-cover tw-z-0' }}
    {%- endif -%}
    <div class="tw-relative tw-p-10 tw-space-y-4 tw-z-10">
      {% if block.settings.tout_headline != blank %}
      <h3 class="tw-text-white tw-h4">{{ block.settings.tout_headline }}</h3>
      {% endif %}
      {% if block.settings.tout_copy != blank %}
      <span class="tw-text-white">
        {{ block.settings.tout_copy }}
      </span>
      {% endif %}
      {% if block.settings.button_link != blank %}
      <a href="{{ block.settings.button_link }}" class="tw-btn tw-btn-green">{{ block.settings.button_cta | escape }}</a>
      {% endif %}
    </div>
  </div>
</div>